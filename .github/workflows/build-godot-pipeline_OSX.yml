# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the develop branch
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  Godot:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform:
          [
            Team_H_Godot_Game_Linux_x64,
            Team_H_Godot_Game_Windows_x64,
            Team_H_Godot_Game_OSX,
          ]
    steps:
      - uses: actions/checkout@v2
        with:
          lfs: true
      - name: Build
        id: build
        uses: josephbmanley/build-godot-action@v1.4.1
        with:
          name: Team_H_Godot_Game
          preset: ${{ matrix.platform }}
          debugMode: "true"
          # Set true to output an artifact zip file
          package: "true"
          # Location of Godot project in repository
          projectDir: "./Team_H_Project"
      - name: Upload Artifact
        uses: actions/upload-artifact@v2
        with:
          name: Client - ${{ matrix.platform }}
          path: ${{ github.workspace }}/${{ steps.build.outputs.build }}
